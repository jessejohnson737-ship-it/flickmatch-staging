## Frontend
#
# Optional: URL origin where your TMDB proxy is deployed.
# If unset, the app will use Hugging Face runtime variables (TMDB_PROXY_BASE) if available,
# otherwise it will default to same-origin.
#
# Example:
# VITE_TMDB_PROXY_BASE="https://your-worker.your-subdomain.workers.dev"
VITE_TMDB_PROXY_BASE=""

